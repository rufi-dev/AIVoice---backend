PORT=5000
OPENAI_API_KEY=your_openai_api_key_here
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM
ELEVENLABS_MODEL_ID=eleven_turbo_v2

# MongoDB Connection
# For local MongoDB:
MONGODB_URI=mongodb://localhost:27017/aivoice
# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/aivoice?retryWrites=true&w=majority

# JWT Secret (change this to a random string in production)
JWT_SECRET=rufialiyev2005
JWT_EXPIRES_IN=7d

# Frontend URL (for CORS - set this to your Netlify frontend URL in production)
# For local development:
FRONTEND_URL=http://localhost:3000
# For production (example):
# FRONTEND_URL=https://your-app.netlify.app

# --- LiveKit (Realtime / WebRTC) ---
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=your_livekit_api_key
LIVEKIT_API_SECRET=your_livekit_api_secret

# Optional: create rooms via RoomService (best-effort)
# LIVEKIT_CREATE_ROOM=1

# Optional: enable LiveKit Egress mixed recordings (best-effort; requires S3 env below)
# LIVEKIT_EGRESS_ENABLED=1

# --- Python Worker (Auto-started) ---
# The Python worker (agent-worker/voice_agent.py) is now automatically started
# when you press "Start" in the UI. No need to manually run it!
# Room names are automatically generated as: room_<agentId>
# Optional: VAD (Voice Activity Detection) - 'off' or 'silero' (requires onnxruntime)
# 'silero' enables barge-in (interruption) - AI stops speaking when user starts talking
# Note: Silero VAD requires Python 3.11/3.12 or Docker (not available on Python 3.14 Windows)
# AGENT_VAD=off
# To enable: AGENT_VAD=silero
AGENT_VAD=silero
# --- S3 (for LiveKit Egress recordings) ---
AWS_REGION=us-east-1
S3_BUCKET=your-recordings-bucket
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key

# --- Twilio (PSTN via SIP trunking) ---
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
